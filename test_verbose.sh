#!/bin/bash
# Quick test script with verbose logging

echo "Running agent-eval with verbose mode..."
echo "This will show you exactly what the API is returning"
echo ""

# Run with verbose flag
agent-eval run --verbose
