# Required: OpenAI API key for the agent
OPENAI_API_KEY=sk-...

# Optional: Use a different model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o

# Optional: Tavily API key for real web search (get free key at tavily.com)
# Without this, the agent uses mock search results
# TAVILY_API_KEY=tvly-...
