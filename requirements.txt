# Core dependencies (required for running evalview)
click>=8.1.0
pydantic>=2.5.0
pyyaml>=6.0
openai>=1.12.0
anthropic>=0.39.0
rich>=13.7.0
httpx>=0.26.0
python-dateutil>=2.8.2
python-dotenv>=1.0.0

# Development dependencies (required for running tests)
# Install with: pip install -e ".[dev]"
# Or install these manually:
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=24.10.0
mypy>=1.7.0
ruff>=0.1.0
