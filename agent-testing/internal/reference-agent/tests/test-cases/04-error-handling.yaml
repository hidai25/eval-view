name: "Error Handling - Invalid City"
description: "Test agent handles invalid input gracefully"

input:
  query: "What's the weather in XYZ123INVALID?"

expected:
  tools:
    - get_weather
  output:
    contains:
      - "error"
      - "not found"

thresholds:
  min_score: 70
  max_cost: 0.01
  max_latency: 1000
