name: "Multi-Tool Sequence - Weather & Conversion"
description: "Test tool sequence with weather lookup and temperature conversion"

input:
  query: "Get the weather for New York and convert the temperature to Celsius"

expected:
  tools:
    - get_weather
    - convert_temperature
  sequence:
    - get_weather
    - convert_temperature
  output:
    contains:
      - "temperature"

thresholds:
  min_score: 70
  max_cost: 0.02
  max_latency: 2000
