name: "Weather Query - Single Tool"
description: "Test weather lookup for a valid city"

input:
  query: "What's the weather in New York?"

expected:
  tools:
    - get_weather
  output:
    contains:
      - "72"
      - "sunny"

thresholds:
  min_score: 75
  max_cost: 0.01
  max_latency: 1000
