name: "Stock Price Query"
description: "Test stock price lookup tool"

input:
  query: "What's the current price of AAPL stock?"

expected:
  tools:
    - get_stock_price
  output:
    contains:
      - "178"
      - "price"

thresholds:
  min_score: 75
  max_cost: 0.01
  max_latency: 1000
