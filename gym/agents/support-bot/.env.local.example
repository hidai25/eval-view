# Copy to .env.local and fill in values

# OpenAI API key (required for LLM calls)
OPENAI_API_KEY=sk-your-key-here

# Optional: Override the model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o

# Mock mode: "always", "never", or "auto"
# - always: Never call real APIs (fully deterministic)
# - never: Always use real APIs (fail if no key)
# - auto: Use APIs if available, fall back to mock
EVALVIEW_MOCK_MODE=always
